<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Library</OutputType>
        <TargetFramework>net9.0</TargetFramework>
        <ImplicitUsings>disable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <Version>1.0.11</Version>
    </PropertyGroup>

    <PropertyGroup Condition="'$(Configuration)' == 'Debug'">
        <DefineConstants>ERR</DefineConstants>
    </PropertyGroup>

    <PropertyGroup>
        <AssemblyName>pathmage.ToolKit</AssemblyName>
        <RootNamespace>pathmage.ToolKit</RootNamespace>
        <Title>pathmage.ToolKit</Title> 
        <PackageId>pathmage.ToolKit</PackageId>
        <Authors>Adam Knight (pathmage)</Authors>
        <RepositoryUrl>https://github.com/pathmage-dev/csharp-tool-kit</RepositoryUrl>
        <PackageLicenseFile>LICENSE</PackageLicenseFile>
        <PackageReadmeFile>README.md</PackageReadmeFile>
    </PropertyGroup>

    <ItemGroup>
        <None Include="../LICENSE" Pack="true" PackagePath=""/>
        <None Include="../README.md" Pack="true" PackagePath=""/> 
    </ItemGroup> 

</Project>
